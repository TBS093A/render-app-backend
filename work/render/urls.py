from rest_framework import permissions, routers
from django.urls import path, include, re_path

router = routers.DefaultRouter()

# router.register(r'render', application, basename='render')

urlpatterns = [
    # path('', include( router.urls ))
]